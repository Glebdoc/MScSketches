# Welcome to my LLM

# .json files store the design parameters:
	Important points:
		"AIRCRAFT":  - supports "drone", "helicopter", "quadcopter" configurations; so pick the one you need 

		"output_dir:" - ensure that's the folder where this .json is stored; so for each case a new .json is created

